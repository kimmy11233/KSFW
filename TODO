X? Better planning agent
X Scroll is forced to end of messages on each message poll even if nothing changes. This can make it hard to read messages when they are super long.
- Story saving (X), loading (X), creating from template (X)
X Embedded story maker
- More involved trigger system
X As text is rendered on the front end it moves making it very hard to read. We want the cursor to start at the top of the screen and write to the bottom. If it is more than a screenful it should write into the space below
X Messages are popping if they are shorter than a screen length
X Checkers
X APplying diffs to frontend
- Add better character mannager (X) Seperate from memory
- Proper Noun system, location and characters, maybe significant items too?
- Better character selection
X Images fuck up their aspect ratio
- Hovering over edited text should show the violation 
- conditions in inventory never expires, update time prediction to give an actual time such that inventory can remove conditions over time.
- Add a little grey errow in the bottom left to move you to the bottom of the story
- The Inventory and Memory buttons should always be availible, but their edit buttons should be locked if the background tasks are working
