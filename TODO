X? Better planning agent
X Scroll is forced to end of messages on each message poll even if nothing changes. This can make it hard to read messages when they are super long.
X Story saving (X), loading (X), creating from template (X)
X Embedded story maker
- More involved trigger system
X As text is rendered on the front end it moves making it very hard to read. We want the cursor to start at the top of the screen and write to the bottom. If it is more than a screenful it should write into the space below
X Messages are popping if they are shorter than a screen length
X Checkers
X APplying diffs to frontend
X Add better character mannager (X) Seperate from memory
- Better character selection
X Images fuck up their aspect ratio
- Hovering over edited text should show the violation 
X conditions in inventory never expires, update time prediction to give an actual time such that inventory can remove conditions over time.
X Add a little grey arrow in the bottom left to move you to the bottom of the story
X The Inventory and Memory buttons should always be availible, but their edit buttons should be locked if the background tasks are working. Editing memory now works(X)
X? Somewhere we lost the grey line above the user mesage
X? When we force the user message up we should make room for the planning sprite, maybe like a few pixels more
X Refactor load/create pipleine with the roleplay and story object
X Proper noun system that reads a chat determins the current proper nouns, which is referenced to actual objects. A generater generates the objects when a new thing comes. 
    - Factions
    - Characters
    - Locations
    - Significant items
X Backup system
- The main system seems to be really rushing and not stopping when the play input is requested, perhaps another agent this one to prepare just a stop beat
X The main agent needs to stop understanding gag speech
X the initial message is never showing
X Fetch the last message and the compiled prompt for an agent by clicking on them
X Update Memory system
    - Events are pretty good
    - Facts are incredibly messy and should be fully replaced with the proper noun system. They are repeating lots of the same thing over and over action and listing a lot of random things 
    - Rules are far too broad. Standing rules are both out of date and not covering long term rules. It shouldn't track restraints
    - Schedual is not super clean, Arangement and access Is neat but again its really only talking about restraints
    - Inventory needs a little work with conditions, conditions are being applied to fast
    - Current state is pretty good
    - Characters is falling out of date

- The gag speech agent is firing far too often
- We need a better way of generating worlds, the seeing and starting process is a little messy.
- Checker system needs to be updated for memory V2
